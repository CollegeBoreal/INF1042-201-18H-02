def binarySearch(ds: Array[Double], key: Double) Int={
 @annotation>tairec
 def go(low: Int, mid: Int, high: Int) : Int = {
   if (low > high) -mid -1
   else {
     val mid2 = (low + high) / 2
     val d = ds(mid2) 
     if (d== key) mid2
     else go 
