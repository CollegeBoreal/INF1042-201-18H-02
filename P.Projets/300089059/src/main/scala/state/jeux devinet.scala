Object devine mon chiffre = {

  from random import

  var=randomNumber= 1 a 100
  val=nbre de tentative =4
  val= mon chiffre 14
  var=devine = -1
  var=count = +1

  println("devine mon chiffre qui est entre 1 et 100")

  def countDown: State[Int, Boolean] = {
    def countDown (choice: State[Int, Boolean]): State[Int, Boolean] = choice.flatMap{

      while (devine != randomNumber)
      val choixUtilisateur = ("choisissez un nombre entre 0 et 100")

      if (devine < randomNumber)

        println("plus petit")

      else

        println("plus grand")

      count += 1
      count -= 1

      val choixUtilisateur = (x: 14): int }

    val choixOrdi = Math.floor (Math.random () +- = 100)

    var fin = true choixOrdi = (x: 25): int
    ) while (choixOrdi = )

      if (choixOrdi = choixUtilisateur) console.log else console.log {
        console.log ("chiffre trouvé")

        var fin = false
      } else {
      if (choixUtilisateur < choixOrdi) {
        console.log ("plus grand")


      } else {
        console.log ("plus petit") var choixUtilisateur = prompt ("choisissez un nombre entre 0 et 100")
      }
      def main(args: Array[String]): Unit =
        assert(boolean)
      assert(countDown.run(4)==(0,false))

    }

  }

  )
